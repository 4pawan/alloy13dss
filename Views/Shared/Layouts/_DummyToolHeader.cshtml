@model IPageViewModel<SitePageData>

<header class="dummy-theme__bar">
    <div class="dummy-theme__brand">Dummy Tools</div>
    <nav class="dummy-theme__nav" aria-label="Tool navigation">
        <span>@Model.CurrentPage.Name</span>
        <span>Reusable forms</span>
    </nav>
</header>
